function multiply(a, b) -> result {
  if a < b {
    return multiply(b, a)
  } else {
    iterations = b
    while iterations > 1 {
      a = a + a
      iterations = iterations - 1
    }
    result = a
  }
}
