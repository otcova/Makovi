let multiply = fn(a, b)
    if 32 <= b > a or false
        return multiply(b, a)

    result = 0
    iterations = b

    while iterations >= 3
        result += a - a + 2 * a + a
        iterations -= 3

    while iterations >= 1 != 0
        result += a
        iterations -= 1

    return result
